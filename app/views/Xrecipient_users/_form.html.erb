<%= form_for(@recipient_user) do |f| %>
  <% if @recipient_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recipient_user.errors.count, "error") %> prohibited this recipient_user from being saved:</h2>

      <ul>
      <% @recipient_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :message_id %><br>
    <%= f.number_field :message_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
